package streamApi;

import java.util.Arrays;

public class StreamTest02 {
	public static void main(String[] args) {
		String[] names = {"짱구", "철수", "훈이", "맹구", "유리"};
		// 배열로 스트림 생성
		// Array.stream()
		// Stream.of()
		
		Arrays.stream(names)args.forEach(System.out::println);
	}
}
